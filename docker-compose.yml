services:
  api:
    build:
      context: ./API
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
  ui:
    build:
      context: ./UI
      dockerfile: Dockerfile
    depends_on:
      - api
    ports:
      - "8001:8000"
